#include <iostream>
#include <fstream>
#include <istream>
using namespace std;

int main() {

  
    ifstream in("main.apexam");     

    while(!in.eof())
    {
    string text;
    istream getline(in, text);

    }

    return 0;
} 